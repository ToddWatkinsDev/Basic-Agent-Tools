# filter_dataset.py
Purpose: Filters a dataset based on a numeric condition.
Inputs: Dataset path, column name, operator (>, <, ==, !=), and value.
Outputs: Saves a filtered CSV to 'filtered_data.csv'.
Usage: Isolating specific data ranges (e.g., TWS > 10).